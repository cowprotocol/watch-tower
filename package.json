{
  "name": "tenderly-watch-tower",
  "version": "0.1.1",
  "description": "A tenderly watch tower, keeping an eye on Composable Cows 👀🐮",
  "author": {
    "name": "Cow Protocol"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "8.39.0",
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^3.4.1",
    "solhint-plugin-prettier": "^0.0.5"
  },
  "scripts": {
    "fmt": "yarn fmt:actions",
    "lint": "yarn lint:actions",
    "build": "yarn build:actions",
    "fmt:actions": "prettier ./actions -w",
    "build:actions": "cd actions && npm ci && yarn run build",
    "lint:actions": "eslint && prettier --check ./actions",
    "test:actions": "yarn build:actions && yarn ts-node actions/test/test_register.ts",
    "start:actions": "yarn ts-node actions/test/run_local.ts",
    "deploy:dev": " tenderly actions deploy --project-config tenderly",
    "deploy:prod": " tenderly actions deploy --project-config tenderly-prod",
    "check-deployment": "yarn build:actions && yarn start:actions"
  },
  "dependencies": {
    "ts-node": "^10.9.1"
  }
}
